<div class="top-window">
    <div class="card-title">
        <h1>Eat like a local</h1>
        <h4>Hey you</h4>
    </div>
    {{!-- {{> navbarprivate}} --}}
    <nav>
        <a href="/myprofile">Home</a>
        <a href="/myprofile/restaurant">All restaurants</a>
        <a href="/myprofile/dashboard">My profile</a>
        <form id="logout-form" action="/auth/signout" method="POST">
            <button>Sign out</button>
        </form>
    </nav>
</div>

<div>
    <h2 class="title-profile">Nice to see you again!</h2>
    <form action="/restaurant/city" method="post" class="get">
        <h2 id="title-form">Where are you going ? </h2>
        <select name="city" style="width:40vw" style="height:10vh;">
            <option value="default">Choose a city</option>
            {{#each cities}}
            <option name="city" value="{{this}}">
                {{this}}
            </option>
            {{/each}}
        </select>
        <button id="search-city" name="submit" value="submit" type="submit"> Go! </button>
    </form>

    <br>

    <h3 class="latest-rec">Latest recommendations</h3>

    {{#each twoRestaurants}}
    <div class="card-resto">
        <div>
            <img class="resto-image" src="{{this.picture}}" alt="picture of {{this.name}}">
        </div>

        <div class="resto-text">
            <h3>{{this.name}}</h3>
            <p>{{this.city}}</p>
            <p>{{this.genre}}</p>
            <p>Recommended by {{this.user.firstname}}</p>
            <p>Added on {{formatDate this.createdAt}}</p>
            <a href="/myprofile/restaurant/{{this.id}}">See more details</a>
        </div>
    </div>
    {{/each}}